import styled from 'styled-components';

const AcceptButtonWrapperStyled = styled.div`
  display: flex;
  justify-content: center;

  margin-top: 72px;
  margin-bottom: 72px;

  > button {
    max-width: 200px;
  }
`;

export default AcceptButtonWrapperStyled;
